# Agent A Instructions

**Role**: Data Services
**Branch**: `agent-a-data`
**Focus**: `src/services/`

## Responsibilities
1. Implement `dataService.ts` to fetch transaction data.
2. Define data types in `src/types/tax.ts` if needed (coordinate with others).
3. Ensure data mocking works for development.

## Checklist
- [ ] Check out `agent-a-data` branch.
- [ ] Implement `fetchTransactions` in `src/services/dataService.ts`.
- [ ] Write unit tests for data fetching.
- [ ] Verify types match `TaxTransaction` interface.
