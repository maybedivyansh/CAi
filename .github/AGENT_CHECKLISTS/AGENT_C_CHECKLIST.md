# Agent C Checklist

- [ ] Review shared types for UI prop requirement.
- [ ] Build reusable components in `src/components/`.
- [ ] Implement Dashboard layout in `src/pages/index.tsx`.
- [ ] Ensure mobile responsiveness.
- [ ] Integrate real data (or mock data) from `dataService` and `taxCalculator`.
